# Frontend env (used at build time for production)
# For same-domain deployment, use /api so the app talks to your server.
# VITE_API_URL=/api

# Go2RTC WebSocket URL (for live camera streams; set when building if using a different host)
# VITE_GO2RTC_WS_URL=wss://your-domain.com/ws