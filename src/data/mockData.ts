import { Vehicle, Camera, Violation } from '@/types/parking';

// Empty arrays - data will be added through the UI
export const mockVehicles: Vehicle[] = [];

export const mockCameras: Camera[] = [];

export const mockViolations: Violation[] = [];
